{
  "name": "reactive-weather",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "idb": "^2.1.1",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "homepage": "https://oleksiyrudenko.github.io/reactive-weather",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "push-gh-pages": "push-dir --dir=build --branch=gh-pages --cleanup --verbose"
  }
}
